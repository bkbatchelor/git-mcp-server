# Track Specification: Add MCP Client Configuration to README

## Overview
Add a section to the README.md file that provides the necessary JSON configuration for MCP clients (like Claude Desktop) to connect to this server.

## Requirements
- Provide a clear JSON example for `mcp.json` or similar client config files.
- Include the command to run the server via `java -jar`.
- Use absolute path placeholders or relative paths where appropriate.