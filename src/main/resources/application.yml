spring:
  application:
    name: git-mcp-server
  main:
    banner-mode: "off"
    lazy-initialization: false
  jmx:
    enabled: false

logging:
  level:
    io.sandboxdev: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n"
